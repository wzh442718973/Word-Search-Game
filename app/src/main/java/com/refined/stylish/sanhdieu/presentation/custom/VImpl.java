package com.refined.stylish.sanhdieu.presentation.custom;
import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import android.content.Context;
import android.content.res.TypedArray;
import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;
import androidx.annotation.Nullable;
import android.util.AttributeSet;

import com.refined.stylish.sanhdieu.R;

 

public class VImpl extends AAIUsedCorrect {
private int roundsArrayUtil_index = 0;
private int zoomAudioIdx = 0;
HashMap<String,Double> nativeLite_dictionary;




    private int mLineWidth;
    private int mColCount;
    private int mRowCount;
    private Paint mPaint;

    public VImpl(Context context) {
        super(context);
        init(context, null);
    }

    public VImpl(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
        init(context, attrs);
    }

    

private HashMap gridGreaterParameters() {
     String playSelected = "seqno";
     String powerFile = "legend";
     int packLocal_jl = 7651;
    HashMap<String,Integer> mvecBimap = new HashMap();
    mvecBimap.put("stringbuffer", playSelected.length());
    mvecBimap.put("letters", powerFile.length());

    return mvecBimap;

}



@Override
    public int getColCount() {

         
HashMap ipmovieRprobe =  this.gridGreaterParameters();

      int ipmovieRprobe_len = ipmovieRprobe.size();
      for(Object object_p : ipmovieRprobe.entrySet()) {
          HashMap.Entry<String, Object> entry = (HashMap.Entry<String, Object>)object_p;
          System.out.println(entry.getKey());
          System.out.println(entry.getValue());
      }



        return mColCount;
    }

    

private boolean recycleOpacitySharedParsersFields(long allCurr, double rememberConnectivity, double interface__Config) {
     long relyContext = 3693L;
     long appNotification = 1272L;
     boolean settingsPools = false;
     ArrayList<Integer> threadSanbox = new ArrayList();
    boolean mponTotally = false;
    relyContext -= 54;
    mponTotally = relyContext > 29;
    appNotification = 45;
    mponTotally = appNotification > 32;
    settingsPools = true;
    mponTotally = settingsPools;

    return mponTotally;

}



@Override
    public int getRowCount() {

         
boolean amountsAsnt =  this.recycleOpacitySharedParsersFields(8386L,7705.0,3682.0);

      if (!amountsAsnt) {
      }



        return mRowCount;
    }

    

private String sleepIntentStubDestroyed() {
     int countReset = 1385;
     HashMap<String,Long> executorAlive = new HashMap();
     long fileBounds = 1460L;
    String appsflyerCroppingProto = "declined";
    if (countReset >= -128 && countReset <= 128){
    int sound_s = Math.min(1, new Random().nextInt(65)) % appsflyerCroppingProto.length();
        appsflyerCroppingProto += countReset + "";
    }
    if (fileBounds <= 128 && fileBounds >= -128){
    int execute_g = Math.min(1, new Random().nextInt(79)) % appsflyerCroppingProto.length();
        appsflyerCroppingProto += fileBounds + "";
    }

    return appsflyerCroppingProto;

}



public int getLineWidth() {

         
String ordertypeBson =  this.sleepIntentStubDestroyed();

      int ordertypeBson_len = ordertypeBson.length();
      System.out.println(ordertypeBson);



        return mLineWidth;
    }

    

private float restoreProcessorProperty() {
     long layoutWord = 1434L;
     int audioCase_4v = 3239;
     int clickLibmplame = 4129;
     double unloadGradlew = 1327.0;
    float fillpInts = 0;
    layoutWord = layoutWord;
    audioCase_4v *= 91;
    clickLibmplame = 5600;
    unloadGradlew += 89;

    return fillpInts;

}



public int getLineColor() {

         
float alignedCandidates =  this.restoreProcessorProperty();

      System.out.println(alignedCandidates);



        return mPaint.getColor();
    }

    

private float wifiMsgPreviouslyJnu(String launcherStarted) {
     HashMap<String,Double> destroyRound = new HashMap();
     int callResult = 5825;
     double attrsAll = 4365.0;
    float soundsSmoothly = 0;
    callResult = callResult;
    attrsAll = 1820;

    return soundsSmoothly;

}



public void setLineWidth(int lineWidth) {

         String strikethroughs_k = "cmov";

float subtractionDeferred =  this.wifiMsgPreviouslyJnu(strikethroughs_k);

      if (subtractionDeferred < 53) {
             System.out.println(subtractionDeferred);
      }



        mLineWidth = lineWidth;
        invalidate();
    }

    

private boolean generateConfigRequired(long packSnap) {
     double makeInterface_1 = 6761.0;
     float streakArray = 917.0f;
     HashMap<String,Float> servicesSize__ = new HashMap();
    boolean vivoAcked = false;
    makeInterface_1 *= 36;
    vivoAcked = makeInterface_1 > 97;
    streakArray *= 24;
    vivoAcked = streakArray > 63;

    return vivoAcked;

}



public void setLineColor(int color) {

         
boolean stickersetVpxdec =  this.generateConfigRequired(8181L);

      if (!stickersetVpxdec) {
      }



        mPaint.setColor(color);
        invalidate();
    }

    

private long openZzfMeasuredNeedObjs() {
     int gamePing = 6156;
     boolean goto_f0Sample = false;
     int directionSplish = 977;
    long intraxdspAllocatePrecompute = 0;
    gamePing = gamePing;
    goto_f0Sample = false;
    intraxdspAllocatePrecompute -= goto_f0Sample ? 60 : 7;
    directionSplish *= 11;

    return intraxdspAllocatePrecompute;

}



@Override
    public int getRequiredWidth() {

         
long thrmatMux =  this.openZzfMeasuredNeedObjs();

      if (thrmatMux > 26) {
             System.out.println(thrmatMux);
      }



        return super.getRequiredWidth() + mLineWidth;
    }

    

private boolean decodeCallbackDaggerPermissionResAndy(int textCompare) {
     float register_tReverse = 5625.0f;
     int liteProvider = 9570;
     double pingGlobal = 7913.0;
     double layoutWrong = 1543.0;
    boolean cmoveObjectivePredictors = false;
    register_tReverse = 3612;
    cmoveObjectivePredictors = register_tReverse > 73;
    liteProvider *= 23;
    cmoveObjectivePredictors = liteProvider > 31;
    pingGlobal = 672;
    cmoveObjectivePredictors = pingGlobal > 64;
    layoutWrong = 1477;
    cmoveObjectivePredictors = layoutWrong > 54;

    return cmoveObjectivePredictors;

}



@Override
    public int getRequiredHeight() {

         
boolean promotedFont =  this.decodeCallbackDaggerPermissionResAndy(166);

      if (!promotedFont) {
          System.out.println("ok");
      }



        return super.getRequiredHeight() + mLineWidth;
    }

    

private boolean telephonyUtfRouter(ArrayList<Float> answeredWord, float sessionBase) {
     float mapsGame = 9662.0f;
     boolean streakStyles = true;
     ArrayList<Integer> ellapsedContract = new ArrayList();
    boolean entitesIntfi = false;
    mapsGame -= mapsGame;
    entitesIntfi = mapsGame > 55;
    streakStyles = false;
    entitesIntfi = streakStyles;

    return entitesIntfi;

}



@Override
    public int getCenterColFromIndex(int cIdx) {

         ArrayList blue_x = new ArrayList();

boolean ubscriberCong =  this.telephonyUtfRouter(blue_x,5033.0f);

      if (ubscriberCong) {
      }



        return super.getCenterColFromIndex(cIdx) + (mLineWidth / 2);
    }

    @Override
    public int getCenterRowFromIndex(int rIdx) {
        return super.getCenterRowFromIndex(rIdx) + (mLineWidth / 2);
    }

    @Override
    public void setColCount(int colCount) {
        mColCount = colCount;
        invalidate();
        requestLayout();
    }

    

private boolean nonCipherStreamAry(float alreadyScale, boolean selectionMethod_7e, float lastUpdate_uw) {
     HashMap<String,Float> scaleAdds = new HashMap();
     double channelAutofill = 2813.0;
     int appUnlock = 3484;
    boolean lockDctxdcCallout = false;
    channelAutofill -= 91;
    lockDctxdcCallout = channelAutofill > 36;
    appUnlock = 704;
    lockDctxdcCallout = appUnlock > 81;

    return lockDctxdcCallout;

}



@Override
    public void setRowCount(int rowCount) {

         
boolean serverEvict =  this.nonCipherStreamAry(5573.0f,true,3138.0f);

      if (serverEvict) {
      }



        mRowCount = rowCount;
        invalidate();
        requestLayout();
    }

    

private boolean ellapsedSubscriberObtainPubCfg(ArrayList<Long> manifestRotation) {
     HashMap<String,String> loginAll = new HashMap();
     float pingThreshold = 8044.0f;
     String arraysProcessor = "tri";
     float indexLast = 4098.0f;
    boolean ncbcTwoDimmed = false;
    pingThreshold = 8512;
    ncbcTwoDimmed = pingThreshold > 68;
    indexLast *= pingThreshold;
    indexLast += indexLast;
    ncbcTwoDimmed = indexLast > 63;

    return ncbcTwoDimmed;

}



@Override
    protected void onDraw(Canvas canvas) {

         ArrayList explicit_w_j = new ArrayList();

boolean sysctlsInternal =  this.ellapsedSubscriberObtainPubCfg(explicit_w_j);

      if (sysctlsInternal) {
      }



        int viewWidth = getRequiredWidth();
        int viewHeight = getRequiredHeight();

        int pLeft = getPaddingLeft();
        int pTop = getPaddingTop();
        int pRight = getPaddingRight();
        int pBottom = getPaddingBottom();

        float y = pTop;
        
        for (int i = 0; i <= getRowCount(); i++) {
            canvas.drawRect(pLeft, y, viewWidth + mLineWidth - pRight, y + mLineWidth, mPaint);

            y += getGridHeight();
        }

        float x = pLeft;
        
        for (int i = 0; i <= getColCount(); i++) {
            canvas.drawRect(x, pTop, x + mLineWidth, viewHeight + mLineWidth - pBottom, mPaint);

            x += getGridWidth();
        }
    }

    private void init(Context context, AttributeSet attrs) {
        mLineWidth = 2;
        mColCount = 8;
        mRowCount = 8;
        mPaint = new Paint(Paint.ANTI_ALIAS_FLAG);

        if (attrs != null) {
            TypedArray a = context.obtainStyledAttributes(attrs, R.styleable.VImpl, 0, 0);

            mLineWidth = a.getDimensionPixelSize(R.styleable.VImpl_libcocosdjsIndex, mLineWidth);
            mPaint.setColor(a.getColor(R.styleable.VImpl_durationCompatInstall, Color.GRAY));
            mColCount = a.getInteger(R.styleable.VImpl_moduleIndexPreference, mColCount);
            mRowCount = a.getInteger(R.styleable.VImpl_processorDelete_gjFormatter, mRowCount);

            a.recycle();
        }
    }
}
