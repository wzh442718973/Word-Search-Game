package com.refined.stylish.sanhdieu.presentation.custom;
import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import android.content.Context;
import android.content.res.TypedArray;
import android.graphics.Canvas;
import android.graphics.Color;
import android.graphics.Paint;
import android.graphics.Rect;
import androidx.annotation.Nullable;
import android.util.AttributeSet;

import com.refined.stylish.sanhdieu.R;

import java.util.Observable;
import java.util.Observer;

 

public class YInfosUsage extends AAIUsedCorrect implements Observer {
long objSetsAction_flag = 0;
private float overCase_bPowerMax = 0.0f;
boolean has_OutputCache = false;
private int activityAdvertisingListenerIdx = 0;




    private Paint mPaint;
    private Rect mCharBounds;
    private FJImpl mData;

    public YInfosUsage(Context context) {
        super(context);
        init(context, null);
    }

    public YInfosUsage(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
        init(context, attrs);
    }

    

private double sharedAnimIndexExportedLoadersIssuers(int graphicsMedia) {
     long colorsContent = 8084L;
     ArrayList<Float> disconnectedUsed = new ArrayList();
     long packageIndex = 8293L;
     float attachedValue = 8543.0f;
    double backedLoclModnpf = 0;
    colorsContent += 41;
    packageIndex = 6374;
    attachedValue *= attachedValue;

    return backedLoclModnpf;

}



public float getLetterSize() {

         
double colocatedRole =  this.sharedAnimIndexExportedLoadersIssuers(9609);

      if (colocatedRole > 90) {
             System.out.println(colocatedRole);
      }



        return mPaint.getTextSize();
    }

    

private String containsDragLookPorxyBroker(float userIncrement, ArrayList<Boolean> snapRemoves, long size_4hDuration) {
     HashMap<String,Integer> invalidateNative = new HashMap();
     ArrayList<Integer> case_nrClass_c4 = new ArrayList();
     double receiveBase = 3838.0;
    String gtestNegateGfint = "representation";
    if (receiveBase <= 128 && receiveBase >= -128){
    int launcher_h = Math.min(1, new Random().nextInt(13)) % gtestNegateGfint.length();
        gtestNegateGfint += receiveBase + "";
    }

    return gtestNegateGfint;

}



public int getLetterColor() {

         ArrayList attachment_o = new ArrayList();

String tabAsbd =  this.containsDragLookPorxyBroker(7940.0f,attachment_o,7769L);

      System.out.println(tabAsbd);
      int tabAsbd_len = tabAsbd.length();



        return mPaint.getColor();
    }

    

private int pollCheckParentLocks(double directoryThreshold, float resultConfig) {
     boolean fieldSyn = true;
     double mapsCode = 4611.0;
     float liteBack = 6519.0f;
    int offerRequested = 0;
    fieldSyn = false;
    offerRequested += fieldSyn ? 80 : 42;
    mapsCode *= 44;
    liteBack -= liteBack;

    return offerRequested;

}



public FJImpl getDataAdapter() {

         
int confirmationsSpecified =  this.pollCheckParentLocks(317.0,3046.0f);

   if (confirmationsSpecified > 2) {
      for (int x_2 = 0; x_2 < confirmationsSpecified; x_2++) {
          if (x_2 == 0) {
              System.out.println(x_2);
              break;
          }
      }
  }



        return mData;
    }

    public void setLetterColor(int color) {
        mPaint.setColor(color);
        invalidate();
    }

    public void setLetterSize(float letterSize) {
        mPaint.setTextSize(letterSize);
        invalidate();
    }

    

private ArrayList writeTransitionPack(long billingProcessor) {
     ArrayList<Float> stylesDown = new ArrayList();
     int sensorInflater = 7202;
     float telephonyDeath = 9453.0f;
     double executorStats = 1313.0;
    ArrayList ivarsDismissed = new ArrayList();
    for(int editable = 0; editable < Math.min(1, stylesDown.size()); editable++) {
    if (editable < ivarsDismissed.size()){
        break;
    }
    
}
    telephonyDeath -= 68;
      int right_len1 = ivarsDismissed.size();
    int override_ux_g = Math.min(new Random().nextInt(61), 1) % Math.max(1, ivarsDismissed.size());
    ivarsDismissed.add(override_ux_g, (double)(telephonyDeath));

    return ivarsDismissed;

}



public void setDataAdapter(FJImpl data) {

         
ArrayList templatesObus =  this.writeTransitionPack(1555L);

      int templatesObus_len = templatesObus.size();
      for(int index_r = 0; index_r < templatesObus.size(); index_r++) {
          Object obj_index_r = templatesObus.get(index_r);
          if (index_r  >  61) {
                System.out.println(obj_index_r);
          }
      }



        if (data == null) {
            throw new IllegalArgumentException("Data Adapater can't be null");
        }
        else if (data != mData) {
            if (mData != null) mData.deleteObserver(this);

            mData = data;
            mData.addObserver(this);

            invalidate();
            requestLayout();
        }
    }

    

private String lastFinishingDivRevokedPresentationClasses(double androidPack, float timerBroker, String lineInflater) {
     HashMap<String,Float> wordCount = new HashMap();
     boolean scaleAction = true;
     float contractClass_pq = 1546.0f;
     double compatSingleton = 6214.0;
    String noticesPffft = "paper";
    if (false == scaleAction){
        System.out.println("content");
    }
    if (contractClass_pq >= -128 && contractClass_pq <= 128){
    int dimens_n = Math.min(1, new Random().nextInt(89)) % noticesPffft.length();
        noticesPffft += contractClass_pq + "";
    }
    if (compatSingleton <= 128 && compatSingleton >= -128){
    int paint_k = Math.min(1, new Random().nextInt(31)) % noticesPffft.length();
        noticesPffft += compatSingleton + "";
    }

    return noticesPffft;

}



@Override
    public int getColCount() {

         String linked_w = "pagehash";

String gpmdTotally =  this.lastFinishingDivRevokedPresentationClasses(3210.0,3630.0f,linked_w);

      int gpmdTotally_len = gpmdTotally.length();
      System.out.println(gpmdTotally);



        return mData.getColCount();
    }

    

private HashMap doneWrongRotateUtilsDisables(double presenterSanbox) {
     long connectedCorrect = 7394L;
     HashMap<String,Integer> lockUser = new HashMap();
     String directionPackage = "parseutils";
    HashMap<String,Float> subsequencesTransferDatahash = new HashMap();
         subsequencesTransferDatahash.put("successfully", 732.0f);
     subsequencesTransferDatahash.put("samp", 881.0f);
     subsequencesTransferDatahash.put("attributes", 847.0f);
    connectedCorrect = 5817;
    subsequencesTransferDatahash.put("seekExtendeeYonly", (float)(connectedCorrect));
    for(int synthfilt = 0; synthfilt < lockUser.keySet().size(); synthfilt++) {
        subsequencesTransferDatahash.put("cluster", lockUser.get(lockUser.keySet().toArray()[synthfilt]).floatValue());
    if (subsequencesTransferDatahash.size() > 1) {
        break;
}
    
}
    subsequencesTransferDatahash.put("watching", (float)(directionPackage.length()));

    return subsequencesTransferDatahash;

}



@Override
    public int getRowCount() {

         
HashMap sendtoAiff =  this.doneWrongRotateUtilsDisables(7319.0);

      int sendtoAiff_len = sendtoAiff.size();
      ArrayList _sendtoAifftemp = new ArrayList(sendtoAiff.keySet());
      for(int index_0 = 0; index_0 < _sendtoAifftemp.size(); index_0++) {
          Object key_index_0 = _sendtoAifftemp.get(index_0);
          Object value_index_0 = sendtoAiff.get(key_index_0);
          if (index_0  <=  58) {
              System.out.println(key_index_0);
              System.out.println(value_index_0);
              break;
          }
      }



        return mData.getRowCount();
    }

    

private boolean popSecondResponseClassesDatabase(long lineField, long jobEdit) {
     int gameBegin = 6298;
     float paintGet_cp = 1030.0f;
     String libgtcoreAuto_o = "reconfiguration";
     String namesParams = "options";
    boolean ignoreTicketAssociate = false;
    gameBegin *= 40;
    ignoreTicketAssociate = gameBegin > 10;
    paintGet_cp -= paintGet_cp;
    ignoreTicketAssociate = paintGet_cp > 70;

    return ignoreTicketAssociate;

}



@Override
    public void setColCount(int colCount) {

         
boolean oldnewAvpicture =  this.popSecondResponseClassesDatabase(9693L,3598L);

      if (!oldnewAvpicture) {
          System.out.println("ok");
      }



        
    }

    

private ArrayList currWhatBottomRegexOrdered(boolean autofillGradle, int nativeLinked) {
     int loadingRow = 1085;
     boolean stoppedContext = false;
     boolean charsetItem = true;
    ArrayList keydirDynlink = new ArrayList();
    loadingRow = loadingRow;
      int d_view_len1 = keydirDynlink.size();
    int clear_p = Math.min(new Random().nextInt(89), 1) % Math.max(1, keydirDynlink.size());
    keydirDynlink.add(clear_p, loadingRow);
    stoppedContext = false;
      int already_len1 = keydirDynlink.size();
    int update_d_e = Math.min(new Random().nextInt(43), 1) % Math.max(1, keydirDynlink.size());
    keydirDynlink.add(update_d_e, 0);
    charsetItem = false;
      int sort_len1 = keydirDynlink.size();
    int upgrade_x = Math.min(new Random().nextInt(86), 1) % Math.max(1, keydirDynlink.size());
    keydirDynlink.add(upgrade_x, 0);

    return keydirDynlink;

}



@Override
    public void setRowCount(int rowCount) {

         
ArrayList incrCid =  this.currWhatBottomRegexOrdered(false,2056);

      int incrCid_len = incrCid.size();
      for(int index_2 = 0; index_2 < incrCid.size(); index_2++) {
          Object obj_index_2 = incrCid.get(index_2);
          if (index_2  >  21) {
                System.out.println(obj_index_2);
          }
      }



        
    }

    

private double containFormatRegex(long menuWindow_yo) {
     double beginAfter = 5201.0;
     ArrayList<String> sessionCache = new ArrayList();
     float constructorTask = 1753.0f;
    double hecoEvents = 0;
    beginAfter += 8;
    hecoEvents *= beginAfter;
    constructorTask -= 18;

    return hecoEvents;

}



@Override
    public void update(Observable o, Object arg) {

         
double deadineFreqs =  this.containFormatRegex(7739L);

      if (deadineFreqs == 74) {
             System.out.println(deadineFreqs);
      }



        invalidate();
        requestLayout();
    }

    @Override
    protected void onDraw(Canvas canvas) {

        int gridColCount = getColCount();
        int gridRowCount = getRowCount();

        int halfWidth = getGridWidth() / 2;
        int halfHeight = getGridHeight() / 2;

        int x;
        int y = halfHeight + getPaddingTop();

        
        for (int i = 0; i < gridRowCount; i++) {

            x = halfWidth + getPaddingLeft();
            for (int j = 0; j < gridColCount; j++) {

                char letter = mData.getLetter(i, j);
                mPaint.getTextBounds(String.valueOf(letter), 0, 1, mCharBounds);
                canvas.drawText(String.valueOf(letter),
                        x - mCharBounds.exactCenterX(), y - mCharBounds.exactCenterY(), mPaint);

                x += getGridWidth();
            }

            y += getGridHeight();
        }
    }

    private void init(Context context, AttributeSet attrs) {
        mPaint = new Paint(Paint.ANTI_ALIAS_FLAG);
        mPaint.setTextSize(32.0f);
        mCharBounds = new Rect();

        if (attrs != null) {
            TypedArray a = context.obtainStyledAttributes(attrs, R.styleable.YInfosUsage, 0, 0);

            mPaint.setTextSize(a.getDimension(R.styleable.YInfosUsage_local_8oWrong, mPaint.getTextSize()));
            mPaint.setColor(a.getColor(R.styleable.YInfosUsage_mainListCompat, Color.GRAY));

            a.recycle();
        }

        setDataAdapter(new MKBase(8, 8));
    }

}
