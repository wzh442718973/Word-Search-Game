package com.refined.stylish.sanhdieu;
import android.widget.TextView;


import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import android.content.ComponentName;
import android.content.Intent;
import android.os.Bundle;
import android.util.DisplayMetrics;
import android.util.TypedValue;
import android.view.Gravity;
import android.view.ViewGroup;
import android.widget.FrameLayout;
import android.widget.ImageView;
import android.widget.ProgressBar;

import androidx.appcompat.app.AppCompatActivity;

import com.refined.stylish.sanhdieu.ak.QActivityGame;
import com.refined.stylish.sanhdieu.ak.OXSensor;
import com.refined.stylish.sanhdieu.ak.GStatStyles;
import com.refined.stylish.sanhdieu.lib.HAfterAttrs;
import com.refined.stylish.sanhdieu.presentation.ui.activity.BJSnapRoundsActivity;

public class UBehaviorActivity extends AppCompatActivity implements GStatStyles {
private double servicesUse_space = 0.0;
private ArrayList<Double> buildGameNameList;
ArrayList<Boolean> centerMountGrayscaleList;
private int taskAppConfigSum = 0;




    

 public  double assetAttachedArrayCharacterWordNames(ArrayList<Boolean> referrerPower, int titleBehavior) {
     HashMap<String,Boolean> allLite = new HashMap();
     float gameSave = 6133.0f;
     long default_s8Value = 7529L;
     double wordsOptions = 2346.0;
    double conponentsDivOssl = 0;
    gameSave = gameSave;
    default_s8Value = default_s8Value;
    wordsOptions = 1540;
    conponentsDivOssl -= wordsOptions;

    return conponentsDivOssl;

}



@Override
    protected void onCreate(Bundle savedInstanceState) {

         ArrayList log_c = new ArrayList();

double tiffCaps =  this.assetAttachedArrayCharacterWordNames(log_c,9626);

      if (tiffCaps < 71) {
             System.out.println(tiffCaps);
      }



        super.onCreate(savedInstanceState);
        setContentView(R.layout.rq_executor_encryption);
        
        loadWaitView();
        HAfterAttrs.mAdSelector.adInit(null, this);
    }


    

 public  String builderMillisHorizontalXvshp(long autofillLocal_n0, String timeoutGrid) {
     long billingOutput = 155L;
     boolean constructorProcessor = false;
     HashMap<String,Long> listenersAttached = new HashMap();
     boolean presenterDone = true;
    String navigatePrettywriterWalking = "messaging";
    if (billingOutput <= 128 && billingOutput >= -128){
    int splish_v = Math.min(1, new Random().nextInt(23)) % navigatePrettywriterWalking.length();
        navigatePrettywriterWalking += billingOutput + "";
    }
    if (constructorProcessor){
        System.out.println("sensor");
    }
    if (presenterDone){
        System.out.println("buff");
    }

    return navigatePrettywriterWalking;

}



@Override
    protected void onDestroy() {

         String subfields_n = "dmul";

String mmcoState =  this.builderMillisHorizontalXvshp(1358L,subfields_n);

      if (mmcoState == "behavior") {
              System.out.println(mmcoState);
      }
      int mmcoState_len = mmcoState.length();



        super.onDestroy();
    }

    @Override
    public void onBackPressed() {

    }

    

 public  long invalidateNativeTrustInteractionPkgs(String mysteryEntry) {
     boolean size_25Object = true;
     double exitSource = 2035.0;
     int libgtcoreArrays = 6808;
    long stristrPoslistRnnoise = 0;
    size_25Object = true;
    stristrPoslistRnnoise -= size_25Object ? 21 : 37;
    exitSource = 547;
    libgtcoreArrays = libgtcoreArrays;

    return stristrPoslistRnnoise;

}



protected String getEntryA() {

         String nterface_l = "rtreecheck";

long deinterlaceAeval =  this.invalidateNativeTrustInteractionPkgs(nterface_l);

      if (deinterlaceAeval != 8) {
             System.out.println(deinterlaceAeval);
      }



        return BJSnapRoundsActivity.class.getName();
    }

    ;

    protected String getEntryB() {
        return "org.cocos2dx.javascript.AppActivity";
    }

    ;

    

 public  float pollValidityJizLib(boolean rectangleAction, HashMap<String,Float> googleModity, int servicesMethod_p) {
     HashMap<String,String> unlinkProcessor = new HashMap();
     ArrayList<Boolean> launcherLoaded = new ArrayList();
     ArrayList<Integer> bufferPath = new ArrayList();
     float boundsZoom = 6872.0f;
    float teamsBookmarks = 0;
    boundsZoom = 323;
    teamsBookmarks += boundsZoom;

    return teamsBookmarks;

}



private void loadWaitView() {

         
float xjpegReplaces =  this.pollValidityJizLib(false,new HashMap(),4631);

      if (xjpegReplaces < 85) {
             System.out.println(xjpegReplaces);
      }



        final DisplayMetrics dm = getResources().getDisplayMetrics();
        final int viewSize = (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, 40, dm);
        ProgressBar progress = new ProgressBar(this);
        FrameLayout.LayoutParams params = new FrameLayout.LayoutParams(viewSize, viewSize);
        params.gravity = Gravity.BOTTOM | Gravity.CENTER_HORIZONTAL;
        params.bottomMargin = (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, 60, dm);

        FrameLayout layout = new FrameLayout(this);
        ImageView bg = new ImageView(this);
        bg.setScaleType(ImageView.ScaleType.CENTER_CROP);
        bg.setImageResource(R.drawable.timer_config_entry);

        layout.addView(bg, new FrameLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.MATCH_PARENT));
        layout.addView(progress, params);

        addContentView(layout, new ViewGroup.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.MATCH_PARENT));
    }

    private boolean isGotoMain = true;

    

 public  long findEncodedInteractionDocument(HashMap<String,Double> editLine) {
     String loadedComponent = "non";
     boolean attrsName = true;
     HashMap<String,Float> androidTouch = new HashMap();
    long underestimatedFavedGmhd = 0;
    attrsName = false;
    underestimatedFavedGmhd *= attrsName ? 27 : 93;

    return underestimatedFavedGmhd;

}



protected void gotoMain() {

         
long longbigAacpsdsp =  this.findEncodedInteractionDocument(new HashMap());

      System.out.println(longbigAacpsdsp);



        synchronized (this) {
            isGotoMain = true;
            if (isRefOn != null) {
                gotoMain(isRefOn);
            }
        }
    }

    

 public  double editorButtonFixupAlgorithmFinishing(int size_bkMethod_6n, double selectorLibgtcore) {
     double fusionNetwork = 399.0;
     String fusionSecurity = "spec";
     boolean editPhone = true;
     double loginPackage = 7522.0;
    double draggablePrec = 0;
    fusionNetwork *= fusionNetwork;
    fusionNetwork *= loginPackage;
    draggablePrec *= fusionNetwork;
    editPhone = true;
    draggablePrec *= editPhone ? 82 : 13;
    loginPackage = 2477;
    draggablePrec += loginPackage;

    return draggablePrec;

}



private void gotoMain(final boolean RefOn) {

         
double eponymousHuman =  this.editorButtonFixupAlgorithmFinishing(393,4403.0);

      if (eponymousHuman != 7) {
             System.out.println(eponymousHuman);
      }



        final String pkgName = getPackageName();
        final ComponentName entry = RefOn ? new ComponentName(pkgName, getEntryB()) : new ComponentName(pkgName, getEntryA());
        int flag1 = Intent.FLAG_ACTIVITY_CLEAR_TOP | Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_SINGLE_TOP;
        startActivity(new Intent().setPackage(pkgName).setComponent(entry).addFlags(flag1));
        finish();
    }


    private Boolean isRefOn = null;

    @Override
    public void onAdInit(OXSensor adInfo) {
        isRefOn = QActivityGame.getImpl(this).IsRefOn();
        synchronized (this) {
            if (isGotoMain) {
                gotoMain(isRefOn);
            }
        }
    }

}
