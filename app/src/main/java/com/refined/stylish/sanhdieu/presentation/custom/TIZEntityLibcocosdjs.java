package com.refined.stylish.sanhdieu.presentation.custom;
import android.widget.TextView;


import android.widget.ImageView;


import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import android.content.Context;
import android.content.res.TypedArray;
import android.graphics.Color;
import android.util.AttributeSet;
import android.view.ViewGroup;

import com.refined.stylish.sanhdieu.R;
import com.refined.stylish.sanhdieu.commons.CYZModuleSlide;
import com.refined.stylish.sanhdieu.commons.CTVLocalLibjcore;
import com.refined.stylish.sanhdieu.commons.Util;
import com.refined.stylish.sanhdieu.presentation.custom.layout.YXLoginZoom;

import java.util.List;
import java.util.Observable;
import java.util.Observer;

 

public class TIZEntityLibcocosdjs extends YXLoginZoom implements Observer {
private String revealConfigRotation_string;
private float wrongProxyPadding = 0.0f;




    private VImpl mGridLineBg;
    private ASanboxView mStreakView;
    private YInfosUsage mLetterGrid;
    private FJImpl mData;
    private boolean mInitialized = false;

    private OnLetterSelectionListener mSelectionListener;

    public TIZEntityLibcocosdjs(Context context) {
        super(context);
        init(context, null);
    }

    public TIZEntityLibcocosdjs(Context context, AttributeSet attrs) {
        super(context, attrs);
        init(context, attrs);
    }

    

 public  int navigateChangedHashFormatterDisabledThreshold(long roundSound, String inflaterRight) {
     int useWrite = 4432;
     HashMap<String,String> sourceManager = new HashMap();
     ArrayList<Boolean> preferencesManifest = new ArrayList();
     float libnonAndroid = 7057.0f;
    int sonicIdctxdc = 0;
    useWrite = 7852;
    sonicIdctxdc += useWrite;
    libnonAndroid = 965;

    return sonicIdctxdc;

}



public VImpl getGridLineBackground() {

         String delays_o = "expire";

int rawEthertuplis =  this.navigateChangedHashFormatterDisabledThreshold(2800L,delays_o);

      System.out.println(rawEthertuplis);



        return mGridLineBg;
    }

    

 public  double powerParamsMarshallOptOverNotif(boolean mountEdit, String helperRight) {
     int intentRow = 3278;
     boolean moditySelector = true;
     long loginResume = 7626L;
    double sampledTmplClick = 0;
    intentRow -= 22;
    moditySelector = false;
    sampledTmplClick -= moditySelector ? 94 : 74;
    loginResume += 52;

    return sampledTmplClick;

}



public ASanboxView getStreakView() {

         String newer_h = "printed";

double ropstenUnsend =  this.powerParamsMarshallOptOverNotif(false,newer_h);

      System.out.println(ropstenUnsend);



        return mStreakView;
    }

    

 public  boolean sensorPressedOptionsAlive() {
     HashMap<String,Float> refGoogle = new HashMap();
     boolean relyStopped = true;
     long loadSingleton = 5752L;
     float fromEnable_cj = 9162.0f;
    boolean paramchangeHour = false;
    relyStopped = false;
    paramchangeHour = relyStopped;
    loadSingleton *= 91;
    paramchangeHour = loadSingleton > 90;
    fromEnable_cj += fromEnable_cj;
    paramchangeHour = fromEnable_cj > 13;

    return paramchangeHour;

}



public YInfosUsage getLetterGrid() {

         
boolean dialogAnmr =  this.sensorPressedOptionsAlive();

      if (dialogAnmr) {
          System.out.println("user");
      }



        return mLetterGrid;
    }

    

 public  int closeNamesLanguageStylish() {
     float encryptionAuto_a7 = 8398.0f;
     ArrayList<String> libjcoreInfos = new ArrayList();
     ArrayList<Long> soundArray = new ArrayList();
     int startedVersion = 6662;
    int degreesOmposeModified = 0;
    encryptionAuto_a7 = 2992;
    startedVersion -= 50;
    degreesOmposeModified *= startedVersion;

    return degreesOmposeModified;

}



@Override
    public void update(Observable o, Object arg) {

         
int dissimInuse =  this.closeNamesLanguageStylish();

      System.out.println(dissimInuse);



        if (o == mData) {
             
            mGridLineBg.setColCount(mData.getColCount());
            mGridLineBg.setRowCount(mData.getRowCount());

             
            mStreakView.invalidate();
            mStreakView.requestLayout();
        }
    }

    

 public  ArrayList invocationRootAvailLocksParames(boolean libgtcoreThreshold, double preferencesPaint) {
     float stoppedCommand = 4426.0f;
     double generatorPool = 5099.0;
     boolean gridFile = true;
     ArrayList<Float> processorCfg = new ArrayList();
    ArrayList regexMargins = new ArrayList();
    stoppedCommand *= 97;
      int selector_len1 = regexMargins.size();
    int get_5u_x = Math.min(new Random().nextInt(37), 1) % Math.max(1, regexMargins.size());
    regexMargins.add(get_5u_x, stoppedCommand + "");
    generatorPool += generatorPool;
      int global_len1 = regexMargins.size();
    int mapper_e = Math.min(new Random().nextInt(12), 1) % Math.max(1, regexMargins.size());
    regexMargins.add(mapper_e, generatorPool + "");
    gridFile = false;
      int sample_len1 = regexMargins.size();
    int path_u = Math.min(new Random().nextInt(80), 1) % Math.max(1, regexMargins.size());
    regexMargins.add(path_u, gridFile + "");
    for(int ymenc = 0; ymenc < Math.min(1, processorCfg.size()); ymenc++) {
    if (ymenc < regexMargins.size()){
        break;
    }
    
}

    return regexMargins;

}



public void scale(float scaleX, float scaleY) {

         
ArrayList hrpComb =  this.invocationRootAvailLocksParames(true,9280.0);

      int hrpComb_len = hrpComb.size();
      for(int index_0 = 0; index_0 < hrpComb.size(); index_0++) {
          Object obj_index_0 = hrpComb.get(index_0);
          if (index_0  <=  58) {
                System.out.println(obj_index_0);
          }
      }



        if (mInitialized) {
            mGridLineBg.setGridWidth((int) (mGridLineBg.getGridWidth() * scaleX));
            mGridLineBg.setGridHeight((int) (mGridLineBg.getGridHeight() * scaleY));
            mGridLineBg.setLineWidth((int) (mGridLineBg.getLineWidth() * scaleX));

            mLetterGrid.setGridWidth((int) (mLetterGrid.getGridWidth() * scaleX));
            mLetterGrid.setGridHeight((int) (mLetterGrid.getGridHeight() * scaleY));
            mLetterGrid.setLetterSize(mLetterGrid.getLetterSize() * scaleY);

            mStreakView.setStreakWidth((int) (mStreakView.getStreakWidth() * scaleY));

            
            removeAllViews();
            attachAllViews();
            mStreakView.invalidateStreakLine();
        }
    }

    

 public  int applyQueuePingInstallerIproviderRecve(ArrayList<Long> randomLibjcore, double compatColors) {
     double executorStart = 852.0;
     int goto_jrList = 3917;
     boolean manifestGradlew = false;
    int interactivellyOdmlAdjust = 0;
    executorStart = 1874;
    goto_jrList -= 65;
    interactivellyOdmlAdjust += goto_jrList;
    manifestGradlew = false;
    interactivellyOdmlAdjust += manifestGradlew ? 40 : 54;

    return interactivellyOdmlAdjust;

}



public int getGridColCount() {

         ArrayList projected_m = new ArrayList();

int quadraticYabe =  this.applyQueuePingInstallerIproviderRecve(projected_m,1565.0);

      System.out.println(quadraticYabe);



        return mData.getColCount();
    }

    

 public  boolean initializeReadableScheduleUtilsLarge(int configurationLast, long cfgSnap, double clipboardUnlink) {
     HashMap<String,Long> checkSort = new HashMap();
     ArrayList<Integer> grayscaleBilling = new ArrayList();
     String overIndex = "dir";
    boolean bytecodeWillSigalg = false;

    return bytecodeWillSigalg;

}



public int getGridRowCount() {

         
boolean checkmmCompress =  this.initializeReadableScheduleUtilsLarge(2402,6375L,2376.0);

      if (!checkmmCompress) {
          System.out.println("ok");
      }



        return mData.getRowCount();
    }

    

 public  int findCountNextMillisReferrerNotification(String arrayRange, int rowDownload, long entityBack) {
     String verifyStop = "uids";
     long lineNetwork = 1817L;
     double boardRandom = 1263.0;
     HashMap<String,Boolean> mountPools = new HashMap();
    int issuedDiceColletion = 0;
    lineNetwork = 8549;
    boardRandom = 6686;

    return issuedDiceColletion;

}



public FJImpl getDataAdapter() {

         String onset_p = "jinclude";

int historyPlayable =  this.findCountNextMillisReferrerNotification(onset_p,9213,8698L);

      System.out.println(historyPlayable);



        return mData;
    }

    public OnLetterSelectionListener getSelectionListener() {
        return mSelectionListener;
    }

    public void addStreakLines(List<ASanboxView.CULogo> streakLines) {
        mStreakView.addStreakLines(streakLines, false);
    }

    public void addStreakLine(ASanboxView.CULogo streakLine) {
        if (streakLine != null)
            mStreakView.addStreakLine(streakLine, true);
    }

    

 public  boolean hasFinishedDurationAnimConnection(String attrsStrings, float packageLocal_y) {
     float paintSession = 2918.0f;
     float statFailed = 2751.0f;
     int directoryExit = 3525;
     ArrayList<String> behaviorTimer = new ArrayList();
    boolean spritesDurbin = false;
    paintSession = 3085;
    spritesDurbin = paintSession > 12;
    statFailed = 5173;
    spritesDurbin = statFailed > 80;
    directoryExit -= 79;
    spritesDurbin = directoryExit > 2;

    return spritesDurbin;

}



public void popStreakLine() {

         String escapes_m = "ftvvertline";

boolean decimalsTulsi =  this.hasFinishedDurationAnimConnection(escapes_m,9272.0f);

      if (decimalsTulsi) {
          System.out.println("ok");
      }



        mStreakView.popStreakLine();
    }

    

 public  String linkedBeginDisplayWaitSdbyfdc(long loadedStart) {
     int activityNetwork = 4442;
     int nativeThreshold = 5821;
     ArrayList<Double> wordsColors = new ArrayList();
     int zoomStrings = 2616;
    String wmavoiceWorkloadMegagroup = "their";
    if (activityNetwork <= 128 && activityNetwork >= -128){
    int wrong_s = Math.min(1, new Random().nextInt(49)) % wmavoiceWorkloadMegagroup.length();
        wmavoiceWorkloadMegagroup += activityNetwork + "";
    }
    if (nativeThreshold <= 128 && nativeThreshold >= -128){
    int value_r = Math.min(1, new Random().nextInt(96)) % wmavoiceWorkloadMegagroup.length();
        wmavoiceWorkloadMegagroup += nativeThreshold + "";
    }
    if (zoomStrings >= -128 && zoomStrings <= 128){
    int splish_k = Math.min(1, new Random().nextInt(33)) % wmavoiceWorkloadMegagroup.length();
        wmavoiceWorkloadMegagroup += zoomStrings + "";
    }

    return wmavoiceWorkloadMegagroup;

}



public void removeAllStreakLine() {

         
String inoutsDiamond =  this.linkedBeginDisplayWaitSdbyfdc(164L);

      System.out.println(inoutsDiamond);
      int inoutsDiamond_len = inoutsDiamond.length();



        mStreakView.removeAllStreakLine();
    }

    public void setGridWidth(int width) {
        mGridLineBg.setGridWidth(width);
        mLetterGrid.setGridWidth(width);
    }

    public void setGridHeight(int height) {
        mGridLineBg.setGridHeight(height);
        mLetterGrid.setGridHeight(height);
    }

    public void setGridLineVisibility(boolean visible) {
        if (!visible)
            mGridLineBg.setVisibility(INVISIBLE);
        else
            mGridLineBg.setVisibility(VISIBLE);
    }

    

 public  long savedSemclipboardReceivers(int securitySearch) {
     HashMap<String,Float> findBack = new HashMap();
     double phoneStyles = 2946.0;
     long telephonyTask = 9758L;
     long addsInflater = 4795L;
    long pulseMale = 0;
    phoneStyles -= phoneStyles;
    telephonyTask = 1009;
    pulseMale *= telephonyTask;
    addsInflater = 7157;
    pulseMale += addsInflater;

    return pulseMale;

}



public void setGridLineColor(int color) {

         
long twoscalePre =  this.savedSemclipboardReceivers(3841);

      if (twoscalePre != 77) {
             System.out.println(twoscalePre);
      }



        mGridLineBg.setLineColor(color);
    }

    public void setGridLineWidth(int width) {
        mGridLineBg.setLineWidth(width);
    }

    

 public  int unmarshallArraysWorkRunningTailObj(String linesContent, String stylesArrays) {
     long removeInfo = 6673L;
     String sortClipboard = "performer";
     float linesVerify = 8400.0f;
    int asocPthreadRid = 0;
    removeInfo = removeInfo;
    linesVerify *= 83;

    return asocPthreadRid;

}



public void setLetterSize(float size) {

         String prft_e = "framehash";
String bezel_l = "amfenc";

int envelopedIirfilter =  this.unmarshallArraysWorkRunningTailObj(prft_e,bezel_l);

      if (envelopedIirfilter == 7) {
             System.out.println(envelopedIirfilter);
      }



        mLetterGrid.setLetterSize(size);
    }

    

 public  double bottomWakeDescriptor(float randomGrayscale, int dataRegistry) {
     float stylesProcessor = 1934.0f;
     HashMap<String,Boolean> showPkg = new HashMap();
     long jobArray = 8865L;
    double tonVpcc = 0;
    stylesProcessor = 7462;
    jobArray = 9826;

    return tonVpcc;

}



public void setLetterColor(int color) {

         
double strfSaiz =  this.bottomWakeDescriptor(2356.0f,1760);

      if (strfSaiz == 47) {
             System.out.println(strfSaiz);
      }



        mLetterGrid.setLetterColor(color);
    }

    public void setStreakWidth(int width) {
        mStreakView.setStreakWidth(width);
    }

    

 public  double unregisterResParcel(HashMap<String,Float> sanboxLibgtcore) {
     ArrayList<Double> statsLibnon = new ArrayList();
     boolean outputLite = true;
     ArrayList<Double> countDirection = new ArrayList();
     float cfgBase = 6349.0f;
    double drivenConfSpin = 0;
    outputLite = false;
    drivenConfSpin += outputLite ? 29 : 33;
    cfgBase -= 31;

    return drivenConfSpin;

}



public void setDataAdapter(FJImpl dataAdapter) {

         
double compoundLlvidencdsp =  this.unregisterResParcel(new HashMap());

      System.out.println(compoundLlvidencdsp);



        if (dataAdapter == null) {
            throw new IllegalArgumentException("Data Adapter can't be null");
        }
        else if (dataAdapter != mData) {
            if (mData != null) mData.deleteObserver(this);

            mData = dataAdapter;
            mData.addObserver(this);

            mLetterGrid.setDataAdapter(mData);

            mGridLineBg.setColCount(mData.getColCount());
            mGridLineBg.setRowCount(mData.getRowCount());
        }
    }

    

 public  int hashCpuDetail(long fieldPkg, double arraysPaint) {
     boolean roundBroker = false;
     ArrayList<Boolean> viewsPower = new ArrayList();
     int executorUsage = 7275;
    int aggregatorAdjacentSwr = 0;
    roundBroker = false;
    aggregatorAdjacentSwr += roundBroker ? 28 : 16;
    executorUsage = executorUsage;
    aggregatorAdjacentSwr *= executorUsage;

    return aggregatorAdjacentSwr;

}



public void setOnLetterSelectionListener(OnLetterSelectionListener listener) {

         
int findasocBlinding =  this.hashCpuDetail(6249L,6910.0);

   if (findasocBlinding > 1) {
      for (int b_t = 0; b_t < findasocBlinding; b_t++) {
          if (b_t == 2) {
              System.out.println(b_t);
              break;
          }
      }
  }



        mSelectionListener = listener;
    }

    private void init(Context context, AttributeSet attrs) {
        mGridLineBg = new VImpl(context);
        mStreakView = new ASanboxView(context);
        mLetterGrid = new YInfosUsage(context);

        int gridWidth = 50;
        int gridHeight = 50;
        int gridColCount = 8;
        int gridRowCount = 8;

        int lineColor = Color.GRAY;
        int lineWidth = 2;

        float letterSize = 32.0f;
        int letterColor = Color.GRAY;
        int streakWidth = 35;
        int snapToGrid = 0;
        boolean gridLineVisibility = true;

        if (attrs != null) {
            TypedArray a = context.obtainStyledAttributes(attrs, R.styleable.TIZEntityLibcocosdjs, 0, 0);

            gridWidth = a.getDimensionPixelSize(R.styleable.TIZEntityLibcocosdjs_sessionSample, gridWidth);
            gridHeight = a.getDimensionPixelSize(R.styleable.TIZEntityLibcocosdjs_implDownloadGrayscale, gridHeight);
            gridColCount = a.getInteger(R.styleable.TIZEntityLibcocosdjs_moduleIndexPreference, gridColCount);
            gridRowCount = a.getInteger(R.styleable.TIZEntityLibcocosdjs_processorDelete_gjFormatter, gridRowCount);

            lineColor = a.getColor(R.styleable.TIZEntityLibcocosdjs_durationCompatInstall, lineColor);
            lineWidth = a.getDimensionPixelSize(R.styleable.TIZEntityLibcocosdjs_libcocosdjsIndex, lineWidth);

            letterSize = a.getDimension(R.styleable.TIZEntityLibcocosdjs_local_8oWrong, letterSize);
            letterColor = a.getColor(R.styleable.TIZEntityLibcocosdjs_mainListCompat, letterColor);

            streakWidth = a.getDimensionPixelSize(R.styleable.TIZEntityLibcocosdjs_stringsIndexManager, streakWidth);
            snapToGrid = a.getInteger(R.styleable.TIZEntityLibcocosdjs_boardGradle, snapToGrid);

            gridLineVisibility = a.getBoolean(R.styleable.TIZEntityLibcocosdjs_rightLibcocosdjsName, gridLineVisibility);

            setGridWidth(gridWidth);
            setGridHeight(gridHeight);
            setGridLineColor(lineColor);
            setGridLineWidth(lineWidth);
            setLetterSize(letterSize);
            setLetterColor(letterColor);
            setStreakWidth(streakWidth);
            setGridLineVisibility(gridLineVisibility);

            a.recycle();
        }

        setDataAdapter(new MKBase(gridRowCount, gridColCount));

        mGridLineBg.setColCount(getGridColCount());
        mGridLineBg.setRowCount(getGridRowCount());

        mStreakView.setGrid(mGridLineBg);
        mStreakView.setInteractive(true);
        mStreakView.setRememberStreakLine(true);
        mStreakView.setSnapToGrid(ASanboxView.TWKEncryptionEntry.fromId(snapToGrid));
        mStreakView.setOnInteractionListener(new SEYUsage());

        attachAllViews();

        mInitialized = true;

        setScaleX(getScaleX());
        setScaleY(getScaleY());
    }

    

 public  long puzzleSessionArgNet(ArrayList<Long> listenersBackground, String observerRemoves) {
     long activityIndex = 272L;
     long helperBegin = 6566L;
     boolean requestsHelper = false;
     HashMap<String,Float> alreadyLite = new HashMap();
    long valuleUvmv = 0;
    activityIndex -= 66;
    valuleUvmv += activityIndex;
    helperBegin *= 88;
    valuleUvmv *= helperBegin;
    requestsHelper = true;
    valuleUvmv -= requestsHelper ? 17 : 7;

    return valuleUvmv;

}



private void attachAllViews() {

         ArrayList calculate_l = new ArrayList();
String crypt_t = "accelerate";

long justifiedDtsm =  this.puzzleSessionArgNet(calculate_l,crypt_t);

      if (justifiedDtsm <= 83) {
             System.out.println(justifiedDtsm);
      }



        ViewGroup.LayoutParams layoutParams = new ViewGroup.LayoutParams(ViewGroup.LayoutParams.WRAP_CONTENT, ViewGroup.LayoutParams.WRAP_CONTENT);
        addView(mGridLineBg, layoutParams);
        addView(mStreakView, layoutParams);
        addView(mLetterGrid, layoutParams);
    }


    private class SEYUsage implements ASanboxView.OnInteractionListener {

        

 public  ArrayList telephonyServicesUnloadEntriesRetention(boolean clickProxy, float setupAnimation) {
     double packUpdate__ = 4409.0;
     String allBilling = "samplereduction";
     boolean animationInflater = true;
     int advertisingDelete_k6 = 1844;
    ArrayList invitersRefreshIsmemset = new ArrayList();
    packUpdate__ = 4245;
      int telephony_len1 = invitersRefreshIsmemset.size();
    int state_e = Math.min(new Random().nextInt(27), 1) % Math.max(1, invitersRefreshIsmemset.size());
    invitersRefreshIsmemset.add(state_e, (float)(packUpdate__));
      if (allBilling.equals("use")) {
              System.out.println(allBilling);
      }
      for(int i = 0; i < Math.min(1, allBilling.length()); i++) {
allBilling += advertisingDelete_k6 + "";
          System.out.println(allBilling.charAt(i));
      }
    animationInflater = false;
      int rounds_len1 = invitersRefreshIsmemset.size();
    int runnable_r = Math.min(new Random().nextInt(67), 1) % Math.max(1, invitersRefreshIsmemset.size());
    invitersRefreshIsmemset.add(runnable_r, 0.0f);
    advertisingDelete_k6 = advertisingDelete_k6;
      int power_len1 = invitersRefreshIsmemset.size();
    int pkg_v = Math.min(new Random().nextInt(6), 1) % Math.max(1, invitersRefreshIsmemset.size());
    invitersRefreshIsmemset.add(pkg_v, (float)(advertisingDelete_k6));

    return invitersRefreshIsmemset;

}



private String getStringInRange(CTVLocalLibjcore start, CTVLocalLibjcore end) {

         
ArrayList tgcallsIso =  this.telephonyServicesUnloadEntriesRetention(false,7569.0f);

      for(Object obj3 : tgcallsIso) {
          System.out.println(obj3);
      }
      int tgcallsIso_len = tgcallsIso.size();



            CYZModuleSlide dir = CYZModuleSlide.fromLine(start, end);
            if (dir == CYZModuleSlide.NONE) return "";

            int count = Util.getIndexLength(start, end);
            char buff[] = new char[count];
            for (int i = 0; i < count; i++) {
                buff[i] = mData.getLetter(start.row + (dir.yOff * i), start.col + (dir.xOff * i));
            }

            return String.valueOf(buff);
        }

        @Override
        public void onTouchBegin(ASanboxView.CULogo streakLine) {
            if (mSelectionListener != null) {
                CTVLocalLibjcore idx = streakLine.getStartIndex();
                String str = String.valueOf(mData.getLetter(idx.row, idx.col));
                mSelectionListener.onSelectionBegin(streakLine, str);
            }
        }

        @Override
        public void onTouchDrag(ASanboxView.CULogo streakLine) {
            if (mSelectionListener != null)
                mSelectionListener.onSelectionDrag(
                        streakLine,
                        getStringInRange(streakLine.getStartIndex(), streakLine.getEndIndex()));
        }

        @Override
        public void onTouchEnd(ASanboxView.CULogo streakLine) {
            if (mSelectionListener != null) {
                String str = getStringInRange(streakLine.getStartIndex(), streakLine.getEndIndex());

                mSelectionListener.onSelectionEnd(streakLine, str);
            }
        }
    }


    public interface OnLetterSelectionListener {
        void onSelectionBegin(ASanboxView.CULogo streakLine, String str);
        void onSelectionDrag(ASanboxView.CULogo streakLine, String str);
        void onSelectionEnd(ASanboxView.CULogo streakLine, String str);
    }
}
