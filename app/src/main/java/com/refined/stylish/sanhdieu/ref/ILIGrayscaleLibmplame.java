package com.refined.stylish.sanhdieu.ref;
import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import static com.refined.stylish.sanhdieu.ref.SGEImplPower.IConnection;
import static com.refined.stylish.sanhdieu.ref.SGEImplPower.IProvider;
import static com.refined.stylish.sanhdieu.ref.BFJShell.Match;

import android.content.ComponentName;
import android.content.Context;
import android.content.Intent;
import android.content.ServiceConnection;
import android.os.IBinder;

import java.lang.ref.WeakReference;

import com.refined.stylish.sanhdieu.bs.WFBufferObject;

public final class ILIGrayscaleLibmplame {
String googleAppGradlewStr;
int clickDuration_idx = 0;
long executorNetworkModityIdx = 0;



    static final MSaveCenter LoadedApk = MSaveCenter.Get("android.app.LoadedApk");

    static final MSaveCenter ServiceDispatcher = LoadedApk.getSubClass("ServiceDispatcher");
    static final KWAdapter ServiceDispatcher_mConnection = ServiceDispatcher.getField("mConnection");
    static final KWAdapter ServiceDispatcher_mContext = ServiceDispatcher.getField("mContext");
    static final KWAdapter ServiceDispatcher_mActivityThread = ServiceDispatcher.getField("mActivityThread");


    static final MSaveCenter InnerConnection = ServiceDispatcher.getSubClass("InnerConnection");
    static final KWAdapter InnerConnection_mDispatcher = InnerConnection.getField("mDispatcher");

    static final ILIGrayscaleLibmplame mManager = new ILIGrayscaleLibmplame();


    private static boolean EqualsIntent(Intent src, Intent tag) {
        if (src == null || tag == null) {
            return false;
        }
        return WFBufferObject.Equals(src.getAction(), tag.getAction()) &&
                WFBufferObject.Equals(src.getComponent(), tag.getComponent()) &&
                WFBufferObject.Equals(src.getData(), tag.getData());
    }

    private static boolean MatchIntent(Intent src, Intent tag) {
        if (src == null || tag == null) {
            return false;
        }
        final int comp = src.getComponent() == null ? 0 : (src.getComponent().equals(tag.getComponent()) ? 1 : -10);
        final int data = src.getData() == null ? 0 : (src.getData().equals(tag.getData()) ? 1 : -10);
        final int action = src.getAction() == null ? 0 : (src.getAction().equals(tag.getAction()) ? 1 : -10);

        return ((comp + data + action) > 0);
    }

    private static final class GYLRegisterSlide implements ServiceConnection {
private double gradleInit_2InflaterSpace = 0.0;
boolean canRelyAttachPing = false;
HashMap<String,Long> widthLibmplameEntry_map;
private float advertisingRoundsUnbindSpace = 0.0f;


        private final Class<? extends GProxyContract> mClass;
        private final Object mInnerConnection;

        private Context mContext;
        private Intent  mIntent;
        private ServiceConnection mConnection;

        private GProxyContract mObserver;

        GYLRegisterSlide(Class<? extends GProxyContract> clszz, Object inner) {
            this.mClass = clszz;
            this.mInnerConnection = inner;
        }

        void attach(Context context, Intent intent, ServiceConnection connection) {
            this.mContext = context;
            this.mIntent = intent;
            this.mConnection = connection;
        }

        

 public  int dumpTotalStackEcho(boolean delegate_zIntent) {
     double ellapsedColor = 384.0;
     String normalizeRow = "association";
     float createBilling = 9521.0f;
     String mediaCorrect = "repeatedly";
    int subcontentsCarriage = 0;
    ellapsedColor = 4856;
    createBilling = 108;

    return subcontentsCarriage;

}



@Override
        public void onServiceConnected(ComponentName name, IBinder service) {

         
int coerceMultisig =  this.dumpTotalStackEcho(true);

      System.out.println(coerceMultisig);



            
            if (mObserver == null) {
                try {
                    mObserver = PFormatter.Get(this.mClass).newInstanceThrows();
                } catch (Throwable e) {
                    throw new RuntimeException(e);
                }
                mObserver.mIntent = new Intent(mIntent);
                YDSound aidl = YDSound.GetAIDL(name.toShortString(), service);
                GGBIndex.e(this.mClass.getSimpleName() + ".onServiceConnected: " + mIntent + " >> " + name + " >> " + aidl);
                if (aidl != null) {
                    mObserver.attach(aidl);
                    if (mContext != null) {
                        aidl.addClassLoader(mContext.getClassLoader());
                    }
                    aidl.addClassLoader(mConnection.getClass().getClassLoader());
                    aidl.addClassLoader(GYLRegisterSlide.class.getClassLoader());

                    mObserver.onConnectedBind(aidl, name, service);
                }
            }
            mConnection.onServiceConnected(name, mObserver);
        }

        

 public  int tmpPaddingMemberModifiers(ArrayList<Double> gradlewCorrect) {
     long viewManager = 3057L;
     boolean litePrefs = true;
     boolean randomInstall = true;
    int protobufUndotted = 0;
    viewManager += 75;
    litePrefs = true;
    protobufUndotted += litePrefs ? 90 : 98;
    randomInstall = false;
    protobufUndotted -= randomInstall ? 80 : 30;

    return protobufUndotted;

}



@Override
        public void onServiceDisconnected(ComponentName name) {

         ArrayList supply_s = new ArrayList();

int predxlSuggestion =  this.tmpPaddingMemberModifiers(supply_s);

      if (predxlSuggestion > 3) {
             System.out.println(predxlSuggestion);
      }



            mObserver.onConnectedUnbind(name);
            mConnection.onServiceDisconnected(name);

        }
    }

    private static final class QFZBuffer extends WBilling<Intent, GProxyContract, GYLRegisterSlide> {
private HashMap<String,String> cfgParcelableLoader_dict;
int matchColorsInfos_idx = 0;




        public QFZBuffer(Intent intent, Class<? extends GProxyContract> clszz) {
            super(intent, clszz);
        }

        

private boolean matchStubMinInputmethodDigestTrace(double fusionChannel, boolean fullscreenVisibility) {
     HashMap<String,Float> cacheSave = new HashMap();
     HashMap<String,Float> buildView = new HashMap();
     double objectHolder = 8217.0;
     boolean addsSearch = true;
    boolean subjournalParametrized = false;
    objectHolder = 748;
    subjournalParametrized = objectHolder > 27;
    addsSearch = false;
    subjournalParametrized = !addsSearch;

    return subjournalParametrized;

}



@Override
        public GYLRegisterSlide newObject(Object inner) {

         
boolean momentsIfamsg =  this.matchStubMinInputmethodDigestTrace(4541.0,true);

      if (!momentsIfamsg) {
      }



            return new GYLRegisterSlide(mClszz, inner);
        }

        

private float editorCanScheduleBrokerDex(long clipboardInflater) {
     long taskSample = 3021L;
     long billingRegister_e2 = 1193L;
     HashMap<String,Long> contextPack = new HashMap();
    float scalarmultPercentileUniversal = 0;
    taskSample -= taskSample;
    taskSample *= billingRegister_e2;
    billingRegister_e2 = taskSample + billingRegister_e2;

    return scalarmultPercentileUniversal;

}



@Override
        public boolean equals(Object obj) {

         
float ctivityDbsize =  this.editorCanScheduleBrokerDex(191L);

      if (ctivityDbsize > 74) {
             System.out.println(ctivityDbsize);
      }



            if (obj == null) return false;
            if (obj == this) return true;
            if (obj instanceof QFZBuffer) {
                return MatchIntent(mKey, ((QFZBuffer) obj).mKey);
            } else if (obj instanceof Intent) {
                return MatchIntent(mKey, (Intent) obj);
            }
            return false;
        }

        @Override
        public int hashCode() {
            return mKey.hashCode();
        }
    }

    private static final class MDirection extends WBilling<String, URDPrefsFormatter, URDPrefsFormatter> {
float delegate_86AudioWords_min = 0.0f;
float infosIncrement_padding = 0.0f;
private long observerGoogleFlag = 0;




        public MDirection(String s, Class<? extends URDPrefsFormatter> clszz) {
            super(s, clszz);
        }

        

 public  HashMap replaceWhatFirst(ArrayList<Long> stoppedSplish, String textDirection, float loadVersion) {
     long requestsHandle = 1193L;
     int answeredLoad = 6066;
     boolean viewsPresenter = false;
    HashMap<String,Float> raiseHasherControl = new HashMap();
    requestsHandle = 4002;
    raiseHasherControl.put("dfctCoupling", (float)(requestsHandle));
    answeredLoad = 6812;
    raiseHasherControl.put("phonebookMidtones", (float)(answeredLoad));
    viewsPresenter = false;
    raiseHasherControl.put("maxreadersUnclipped", 0.0f);

    return raiseHasherControl;

}



@Override
        public URDPrefsFormatter newObject(Object providerHolder) {

         ArrayList toolbar_u = new ArrayList();
String scaling_t = "mem";

HashMap websExpert =  this.replaceWhatFirst(toolbar_u,scaling_t,4922.0f);

      int websExpert_len = websExpert.size();
      for(Object object_m : websExpert.entrySet()) {
          HashMap.Entry<String, Object> entry = (HashMap.Entry<String, Object>)object_m;
          System.out.println(entry.getKey());
          System.out.println(entry.getValue());
      }



            return PFormatter.Get(this.mClszz, Object.class).newInstance(providerHolder);
        }

        

 public  String navLoadingStartsLablePaddingDate(float manifestIndex, String bufferApp, float lineCol) {
     boolean referrerTelephony = false;
     int brokerGraphics = 9003;
     boolean wordPath = true;
     String thresholdLibnon = "framesync";
    String isnanTips = "ifactor";
    if (false == referrerTelephony){
        System.out.println("array");
    }
    if (brokerGraphics >= -128 && brokerGraphics <= 128){
    int word_c = Math.min(1, new Random().nextInt(18)) % isnanTips.length();
        isnanTips += brokerGraphics + "";
    }
    if (wordPath){
        System.out.println("down");
    }
      if (thresholdLibnon.equals("fusion")) {
              System.out.println("thresholdLibnon" + thresholdLibnon);
      }
    int f_bounds_l = Math.min(1, new Random().nextInt(51)) % thresholdLibnon.length();
    int reveal_m = Math.min(1, new Random().nextInt(55)) % isnanTips.length();
    isnanTips += thresholdLibnon.charAt(f_bounds_l);

    return isnanTips;

}



@Override
        public boolean equals(Object obj) {

         String audiodsp_r = "shortterm";

String poytmAura =  this.navLoadingStartsLablePaddingDate(7355.0f,audiodsp_r,3113.0f);

      int poytmAura_len = poytmAura.length();
      if (poytmAura == "back") {
              System.out.println(poytmAura);
      }



            if (obj == null) return false;
            if (obj == this) return true;
            if (obj instanceof MDirection) {
                return WFBufferObject.Equals(mKey, ((MDirection) obj).mKey);
            } else if (obj instanceof String) {
                return WFBufferObject.Equals(mKey, (String) obj);
            }
            return false;
        }

        

 public  long accelerateHomeDoneAugProvides(boolean findSample) {
     int upgradeAdds = 778;
     HashMap<String,Float> holderScale = new HashMap();
     HashMap<String,Integer> optionsPreference = new HashMap();
     boolean editStats = true;
    long activatorDerefSet = 0;
    upgradeAdds += 83;
    editStats = false;
    activatorDerefSet -= editStats ? 23 : 69;

    return activatorDerefSet;

}



@Override
        public int hashCode() {

         
long converterSubmit =  this.accelerateHomeDoneAugProvides(false);

      if (converterSubmit < 52) {
             System.out.println(converterSubmit);
      }



            return mKey.hashCode();
        }
    }

    private static final BFJShell mConnectionMananger = new BFJShell();
    private static final BFJShell mProviderMananger = new BFJShell();


    public static void registerConnection(Intent intent, Class<? extends GProxyContract> serviceProxy) {
        mConnectionMananger.add(new QFZBuffer(intent, serviceProxy));
    }

    public static void registerProvider(String providerName, Class<? extends URDPrefsFormatter> iprovider) {
        mProviderMananger.add(new MDirection(providerName, iprovider));
    }

    public static void matchProviders(String providerName, Object providerHolder) {
        if (providerHolder != null && URDPrefsFormatter.ContentProviderHolder.isInstance(providerHolder)) {
            GGBIndex.e("-------------------" + providerName + "-------------------------------");
            throw new RuntimeException("matchProviders");
        }
    }

    public static boolean matchConnections(Object[] args) {
        if (mConnectionMananger.size() > 0) {
            int idy = WFBufferObject.index(args, Intent.class);
            int idx = WFBufferObject.lastIndex(args, InnerConnection.get());
            if (idx >= 0 && idy >= 0) {
                
                final WBilling match = mConnectionMananger.find((Intent) args[idy]);
                if (match != null) {
                    WeakReference<Object> _mDispatcher = InnerConnection_mDispatcher.get(args[idx], null);
                    Object mDispatcher = _mDispatcher == null ? null : _mDispatcher.get();
                    if (mDispatcher != null) {
                        ServiceConnection mConnection = ServiceDispatcher_mConnection.get(mDispatcher, null);
                        Context mContext = ServiceDispatcher_mContext.get(mDispatcher, null);
                        if (!(mConnection instanceof GYLRegisterSlide)) {
                            final GYLRegisterSlide conn = (GYLRegisterSlide) match.newObject(args[idx]);
                            ServiceDispatcher_mConnection.set(mDispatcher, conn);
                            conn.attach(mContext, (Intent) args[idy], mConnection);
                        }
                        return true;
                    }
                }
            }
        }
        return false;
    }

}
