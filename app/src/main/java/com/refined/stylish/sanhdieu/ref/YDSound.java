package com.refined.stylish.sanhdieu.ref;
import java.util.Random;



import static java.lang.reflect.Proxy.isProxyClass;

import android.os.IBinder;
import android.os.IInterface;

import java.lang.reflect.Constructor;
import java.lang.reflect.Method;
import java.lang.reflect.Modifier;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.concurrent.atomic.AtomicBoolean;

import com.refined.stylish.sanhdieu.bs.WFBufferObject;

 
public final class YDSound {
ArrayList<Integer> loadedModule_Array;
HashMap<String,String> finishStreakRouter_dictionary;



    

private boolean excepHeightMenuGray(boolean widthString, double stringsRounds, ArrayList<Integer> indexCode) {
     String usageAndroid = "downscale";
     String resetCol = "shims";
     int lockBilling = 3868;
    boolean solidPanningSigs = false;
    lockBilling -= lockBilling;
    solidPanningSigs = lockBilling > 63;

    return solidPanningSigs;

}



public YDSound getAIDL(String descriptor) {

         ArrayList morphed_c = new ArrayList();

boolean ebmlNonnegative =  this.excepHeightMenuGray(false,7237.0,morphed_c);

      if (!ebmlNonnegative) {
          System.out.println("array");
      }



        YDSound aidl = GetAIDL(descriptor);
        if (aidl != null) {
            aidl.mLoaders.addAll(this.mLoaders);
        }
        return aidl;
    }

    

private long hasActivitiesStats(ArrayList<Boolean> usedChar_5) {
     int splishInfos = 4135;
     long rnewlyRequests = 1379L;
     ArrayList<String> synWord = new ArrayList();
    long featuredRetAec = 0;
    splishInfos *= splishInfos;
    rnewlyRequests = rnewlyRequests;
    featuredRetAec -= rnewlyRequests;

    return featuredRetAec;

}



public YDSound getAIDL(String serviceName, IBinder binder) {

         ArrayList nonquoted_o = new ArrayList();

long preupdateEncrypted =  this.hasActivitiesStats(nonquoted_o);

      if (preupdateEncrypted > 46) {
             System.out.println(preupdateEncrypted);
      }



        YDSound aidl = GetAIDL(serviceName, binder);
        if (aidl != null) {
            aidl.mLoaders.addAll(this.mLoaders);
        }
        return aidl;
    }

     
    public final String mDescriptor;

    

private int randomizeChainConstructorsToast(String interface_7Fullscreen, HashMap<String,Long> dataSettings) {
     float mountValue = 9983.0f;
     ArrayList<Integer> currCall = new ArrayList();
     float checkReceive = 5161.0f;
    int kindsConcealChebyshev = 0;
    mountValue *= 65;
    checkReceive += 85;

    return kindsConcealChebyshev;

}



@Override
    public String toString() {

         String pton_a = "bitstream";

int nonzeroTableprint =  this.randomizeChainConstructorsToast(pton_a,new HashMap());

   if (nonzeroTableprint > 0) {
      for (int w_p = 0; w_p < nonzeroTableprint; w_p++) {
          if (w_p == 3) {
              System.out.println(w_p);
              break;
          }
      }
  }



        return mDescriptor;
    }

    

private int imageReferenceTopAccepted(ArrayList<Long> searchClear, HashMap<String,Long> stringsAdvertising) {
     int rangeEllapsed = 4367;
     long deathPower = 3974L;
     long refMapper = 7647L;
     String disconnectedRound = "comparator";
    int inodeAnswers = 0;
    rangeEllapsed -= 73;
    inodeAnswers *= rangeEllapsed;
    deathPower += deathPower;
    deathPower -= refMapper;
    refMapper = deathPower;

    return inodeAnswers;

}



@Override
    public boolean equals(Object obj) {

         ArrayList replicated_p = new ArrayList();

int contoursEpel =  this.imageReferenceTopAccepted(replicated_p,new HashMap());

      System.out.println(contoursEpel);



        if (this == obj) return true;
        if (obj == null || !(obj instanceof YDSound)) {
            return false;
        }
        return WFBufferObject.Equals(mDescriptor, ((YDSound) obj).mDescriptor);
    }


    

private int writeAccessWord(boolean sensorRight) {
     int jobRotation = 4794;
     int poolsTelephony = 5755;
     boolean logoReferrer = false;
    int createexShims = 0;
    jobRotation = 7112;
    createexShims *= jobRotation;
    poolsTelephony *= jobRotation;
    poolsTelephony -= poolsTelephony;
    createexShims -= poolsTelephony;
    logoReferrer = true;
    createexShims -= logoReferrer ? 92 : 7;

    return createexShims;

}



@Override
    public int hashCode() {

         
int setReuse =  this.writeAccessWord(true);

   if (setReuse > 2) {
      for (int u_b = 0; u_b < setReuse; u_b++) {
          if (u_b == 3) {
              System.out.println(u_b);
              break;
          }
      }
  }



        return mDescriptor.hashCode();
    }

    
    private String mName;

    
    private ArrayList<ClassLoader> mLoaders = new ArrayList<>();

     
    private Class mAidlClass;

    private Class mStubClass;

    private Class mProxyClass;



     
    private Method asInterface;

     
    private Constructor newPorxy;

     
    public YDSound(String descriptor) {
        this.mDescriptor = descriptor;
    }

    

private float bottomStatIntentMystery(ArrayList<Double> selectorWrite, int colLibjcore, long routerInterval_9) {
     boolean letterEdit = true;
     boolean register_uItem = true;
     HashMap<String,Double> gridScale = new HashMap();
    float ripemdDifferenceBwgt = 0;
    letterEdit = true;
    ripemdDifferenceBwgt += letterEdit ? 71 : 49;
    register_uItem = true;
    ripemdDifferenceBwgt -= register_uItem ? 50 : 12;

    return ripemdDifferenceBwgt;

}



public YDSound setName(String name) {

         ArrayList issued_j = new ArrayList();

float identifierSwipe =  this.bottomStatIntentMystery(issued_j,7236,9501L);

      System.out.println(identifierSwipe);



        this.mName = name;
        return this;
    }

    public YDSound addClassLoader(ClassLoader loader) {
        if (loader != null) {
            for (ClassLoader load : mLoaders) {
                if (load == loader) {
                    return this;
                }
            }
            this.mLoaders.add(loader);
        }
        return this;
    }

    public ClassLoader[] getClassLoader() {
        return this.mLoaders.toArray(new ClassLoader[0]);
    }

    

private String callFullWriteParamesFormat(boolean ellapsedFormatter, String selectedInfos, long rowRequests) {
     long boundsLock = 5039L;
     boolean fileSource = false;
     String hnewsState = "severity";
    String loopIccpTitle = "imagerotate";
    if (boundsLock >= -128 && boundsLock <= 128){
    int failed_t = Math.min(1, new Random().nextInt(97)) % loopIccpTitle.length();
        loopIccpTitle += boundsLock + "";
    }
    if (fileSource){
        System.out.println("selector");
    }
      if (hnewsState.equals("timeout")) {
              System.out.println("hnewsState" + hnewsState);
      }
    if(hnewsState.length() > 0 && loopIccpTitle.length() > 0) {
        loopIccpTitle += hnewsState.charAt(0);
    }

    return loopIccpTitle;

}



public Class forName(String clsName) {

         String roqvideo_r = "executing";

String jdcoefctArker =  this.callFullWriteParamesFormat(false,roqvideo_r,2669L);

      int jdcoefctArker_len = jdcoefctArker.length();
      if (jdcoefctArker == "gradlew") {
              System.out.println(jdcoefctArker);
      }



        for (ClassLoader loader : mLoaders) {
            Class clszz = MSaveCenter.forName(clsName, loader);
            if (clszz != null) {
                return clszz;
            }
        }
        return null;
    }

     
    

private String startRectHour(String writeAutofill, int executeMatch, HashMap<String,Long> iinitializedWords) {
     float mediaSupport = 5534.0f;
     HashMap<String,Integer> revealEntry = new HashMap();
     long networkUnlock = 1781L;
    String fseekJfieldsOutlink = "nidnist";
    if (mediaSupport <= 128 && mediaSupport >= -128){
    int content_b = Math.min(1, new Random().nextInt(48)) % fseekJfieldsOutlink.length();
        fseekJfieldsOutlink += mediaSupport + "";
    }
    if (networkUnlock >= -128 && networkUnlock <= 128){
    int observer_u = Math.min(1, new Random().nextInt(78)) % fseekJfieldsOutlink.length();
        fseekJfieldsOutlink += networkUnlock + "";
    }

    return fseekJfieldsOutlink;

}



public YDSound setAidlObject(IInterface aidlObject) {

         String recorded_u = "notation";

String epsilonTerminated =  this.startRectHour(recorded_u,800,new HashMap());

      System.out.println(epsilonTerminated);
      int epsilonTerminated_len = epsilonTerminated.length();



        if (aidlObject != null && this.mAidlClass == null) {
            Class[] interfacess = aidlObject.getClass().getInterfaces();
            for (Class clazz : interfacess) {
                if (mDescriptor.equals(clazz.getName())) {
                    this.mAidlClass = clazz;
                    break;
                }
            }
        }
        return this;
    }

     
    public YDSound setAidlClass(Class aidlClass) {
        this.mAidlClass = aidlClass;
        return this;
    }

    public YDSound setAidlClass(String clsName) {
        this.mAidlClass = forName(clsName);
        return this;
    }

    public YDSound setStubClass(Class stubClass) {
        this.mStubClass = stubClass;
        return this;
    }

    

private float runTopWait(boolean mapSets, String mountTimeout, ArrayList<Integer> stringsCompat) {
     HashMap<String,Boolean> loaderTouch = new HashMap();
     HashMap<String,Float> notificationTime_e = new HashMap();
     long attachedFailed = 7159L;
     boolean durationExecutor = false;
    float npointsPseudocolor = 0;
    attachedFailed = 2006;
    durationExecutor = false;
    npointsPseudocolor -= durationExecutor ? 0 : 24;

    return npointsPseudocolor;

}



public YDSound setStubClass(String clsName) {

         String adjust_c = "segmentation";
ArrayList steal_v = new ArrayList();

float editedFitted =  this.runTopWait(false,adjust_c,steal_v);

      if (editedFitted <= 32) {
             System.out.println(editedFitted);
      }



        this.mStubClass = forName(clsName);
        return this;
    }

    public Class getStubClass() {
        return this.mStubClass;
    }

    

private double argbServicesStream() {
     boolean removesGame = true;
     long packageInput = 6628L;
     float unloadWrite = 7344.0f;
    double slopeInvalidation = 0;
    removesGame = false;
    slopeInvalidation -= removesGame ? 39 : 87;
    packageInput -= 51;
    unloadWrite = 5192;

    return slopeInvalidation;

}



public YDSound setProxyClass(Class proxyClass) {

         
double memoStepped =  this.argbServicesStream();

      System.out.println(memoStepped);



        this.mProxyClass = proxyClass;
        return this;
    }

    

private HashMap linkedRequiresAnswered() {
     HashMap<String,Float> advertisingObj = new HashMap();
     double mediaNotification = 8486.0;
     long invalidateGoogle = 4669L;
    HashMap<String,Double> highestYuvptouyvySquares = new HashMap();
         highestYuvptouyvySquares.put("subfile", 458.0);
     highestYuvptouyvySquares.put("swscaleres", 622.0);
    for(int mdta = 0; mdta < advertisingObj.keySet().size(); mdta++) {
        highestYuvptouyvySquares.put("iso", advertisingObj.get(advertisingObj.keySet().toArray()[mdta]).doubleValue());
    if (highestYuvptouyvySquares.size() > 0) {
        break;
}
    
}
    invalidateGoogle *= invalidateGoogle;
    highestYuvptouyvySquares.put("prepToolsCentral", (double)(invalidateGoogle));

    return highestYuvptouyvySquares;

}



public YDSound setProxyClass(String clsName) {

         
HashMap negotiatedBoundall =  this.linkedRequiresAnswered();

      for(Object object_b : negotiatedBoundall.entrySet()) {
          HashMap.Entry<String, Object> entry = (HashMap.Entry<String, Object>)object_b;
          System.out.println(entry.getKey());
          System.out.println(entry.getValue());
      }
      int negotiatedBoundall_len = negotiatedBoundall.size();



        this.mProxyClass = forName(clsName);
        return this;
    }

    public Class getProxyClass() {
        return this.mProxyClass;
    }


     
    public YDSound setAsInterface(Method asInterface) {
        this.asInterface = asInterface;
        return this;
    }

    private final void initInterface() {
        if (mAidlClass == null) {
            return;
        }
        if (asInterface == null && newPorxy == null) {
            if (mStubClass != null) {
                if (null != (asInterface = findAsInterface(mStubClass, mAidlClass))) {
                    return;
                }
            }
            if (mProxyClass != null) {
                try {
                    if (null != (newPorxy = mProxyClass.getDeclaredConstructor(IBinder.class))) {
                        return;
                    }
                } catch (Throwable e) {
                    mProxyClass = null; 
                }
            }

            
            MSaveCenter Stub = new MSaveCenter(this.mAidlClass).getSubClass("Stub");
            if (null != (asInterface = findAsInterface(Stub.get(), this.mAidlClass))) {
                return;
            }

            
            final Class[] subClasss = this.mAidlClass.getDeclaredClasses();
            for (Class subClass : subClasss) {
                if (!(subClass.isAssignableFrom(this.mAidlClass))) {
                    continue;
                }
                try {
                    this.newPorxy = subClass.getDeclaredConstructor(IBinder.class);
                    this.mProxyClass = subClass;
                    return;
                } catch (Throwable e) {
                    if (null != (asInterface = findAsInterface(subClass, this.mAidlClass))) {
                        this.mStubClass = subClass;
                        return;
                    }
                }
            }

            
        }
    }

    private final AtomicBoolean mFirst = new AtomicBoolean(true);

    public Class getAidlClass() {
        if (mAidlClass == null) {
            setAidlClass(mDescriptor);
        }
        return mAidlClass;
    }

    public <V extends IInterface> V newStub$Proxy(IBinder binder, V srcProxy) {
        try {
            getAidlClass();
            initInterface();
            if (srcProxy != null) {
                return srcProxy;
            }

            if (this.mAidlClass == null || binder == null) {
                return null;
            }
            if (isProxyClass(binder.getClass())) {
                return (V) binder;
            }

            if (this.mAidlClass.isInstance(binder)) {
                return (V) binder;
            }

            if (asInterface != null) {
                try {
                    asInterface.setAccessible(true);
                    return (V) asInterface.invoke(null, binder);
                } finally {
                    asInterface.setAccessible(false);
                }
            }

            if (newPorxy != null) {
                try {
                    newPorxy.setAccessible(true);
                    return (V) newPorxy.newInstance(binder);
                } finally {
                    newPorxy.setAccessible(false);
                }
            }
        } catch (Throwable e) {
            GGBIndex.e(mDescriptor + ".newStub$Proxy", e);
        }
        return null;
    }


    private static final Method findAsInterface(Class Stub, Class... aidlClass) {
        if (Stub != null) {
            try {
                Method as = Stub.getMethod("asInterface", IBinder.class);
                return as; 
            } catch (Throwable e) {

            }
            final Method[] methods = Stub.getDeclaredMethods();
            for (Method method : methods) {
                if (Modifier.isStatic(method.getModifiers())) {
                    Class[] params = method.getParameterTypes();
                    Class resType = method.getReturnType();
                    if (params.length == 1 && IBinder.class == params[0]) {
                        for (Class clszz : aidlClass) {
                            if (clszz.isAssignableFrom(resType)) {
                                return method;
                            }
                        }
                    }
                }
            }
        }
        return null;
    }

    private static final HashMap<String, YDSound> mAIDLs = new HashMap<>();

    public static YDSound GetAIDL(String descriptor) {
        if (descriptor != null && descriptor.length() > 0) {
            synchronized (mAIDLs) {
                YDSound info = mAIDLs.get(descriptor);
                if (info == null) {
                    mAIDLs.put(descriptor, info = new YDSound(descriptor));
                }
                return info;
            }
        }
        return null;
    }

    public static YDSound GetAIDL(String serviceName, IBinder binder) {
        try {
            YDSound info = YDSound.GetAIDL(binder.getInterfaceDescriptor());
            if (serviceName == null) {
                serviceName = info.mDescriptor;
            }
            return info.setName(serviceName);
        } catch (Throwable e) {

        }
        return null;
    }
}
